PORT=4000
MONGODB_URI=
CORS_ORIGIN=
JWT_SECRET=
ADMIN_SETUP_TOKEN=

NODE_ENV=development
TRUST_PROXY=
METRICS_TOKEN=

GATE_API_KEY=

# Preferred production email path on Render free because it uses HTTPS, not SMTP.
BREVO_API_KEY=
BREVO_FROM_EMAIL=
BREVO_FROM_NAME=VDL Fulfilment Ops

# SMTP fallback for local development or paid hosts that allow SMTP ports.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TIMEOUT_MS=12000
